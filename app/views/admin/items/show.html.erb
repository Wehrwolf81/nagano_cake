<h1>商品詳細</h1>
<div class="container">
  <div class="row no-gutters">
    <div class="col-md-3 ">
      <%= attachment_image_tag @item, :image_id, :fill, 80, 80, format: 'jpeg', fallback: "no_image.jpg" %>
    </div>
    <div class="col-md-8 ">
      <p>商品名：<%= @item.name %></p>
      <p>商品説明：<%= @item.introduction %></p>
      <p>ジャンル：<%= @item.genre_id %></p>
      <p>税込価格(税抜価格)：<%= @item.price %></p>
      <p>販売ステータス：<%= @item.is_active %></p>
       <%= link_to "編集する", edit_admin_item_path(@item.id),class: 'btn btn-default' %>
    </div>
